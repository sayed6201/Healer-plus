<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.team71.healerplus">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/finalicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/finalicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VectorBroneDiseaseDetailActivity" />
        <activity android:name=".VectorBorneActivity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".InfectiousDiseaseInputActivity" />
        <activity android:name=".TestActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".SpinnerActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.MaterialComponents.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Slide1"
            android:theme="@style/Theme.MaterialComponents.NoActionBar" />
        <activity
            android:name=".LocationActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AboutActivity"
            android:theme="@style/Theme.MaterialComponents.NoActionBar" />
        <activity android:name=".epidemic" />
        <activity android:name=".Disease" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".InfectiousActivity" />
        <activity android:name=".InfectiousDiseaseActivity" />
        <activity android:name=".Emergency" />
        <activity android:name=".RegularActivity" />
        <activity android:name=".GalleryActivity" />
    </application>

</manifest>